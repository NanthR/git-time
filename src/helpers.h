#include<regex>
#include<iostream>

const std::vector<std::string> explode(const std::string& s, const char& c);
std::string rtrim(std::string str);
std::string create_commmand(std::vector<std::string>hashes, std::vector<std::string>dates);
